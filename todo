Interpreter:
	- count ( and ) in raw rules string
	- redesign the checking process:
		- function that checks the raw strings
		- function that checks the Variable.list
		- function that checks the Node.list
	- add a Rule class, with line numbers for error handling
